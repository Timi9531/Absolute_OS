set fish_greeting ""

if status is-interactive
set -gx PATH $HOME/eww/target/release $PATH
alias fucking "sudo"
alias pls "sudo"
alias killnigger "rm"
alias c "clear"
alias cmatrix "cmatrix -C yellow"
alias m "math"
    # Commands to run in interactive sessions can go here
end

fish_add_path /home/Timi/.spicetify
